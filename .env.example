# Top-level .env — only used by the legacy docker-compose.prod.yml (DEPRECATED).
# Production now uses Podman Quadlet on the main 4GB box; see infra/podman/quadlet/.
# Kept for one release as parity reference for local Docker workflows.
WEB_IMAGE=ghcr.io/<github-user>/portfolio:latest
WEB_BIND_ADDR=127.0.0.1
